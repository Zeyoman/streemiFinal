{% extends 'base.html.twig' %}

{% block title %}Modifier {{ category.nom }}{% endblock %}

{% block content %}
    <h1 class="text-3xl font-bold text-center">Modifier la catégorie <span class="text-red-600">{{ category.nom }}</span></h1>

    <a href="{{ path('app_admin_category_index') }}" class="mt-8 p-2 block">&larr; Retour à la liste des catégories</a>

    <div class="mt-5">
        {{ include('admin/category/_form.html.twig', {'button_label': 'Modifier la catégorie'}) }}
    </div>

    {{ include('admin/category/_delete_form.html.twig') }}

    <a href="{{ path('app_admin_category_index') }}" class="mt-8 p-2 block">&larr; Retour à la liste des catégories</a>

    <div class="border-t mt-8 pt-4"></div>

{#    <ul>#}
{#        <li><b>Nombre de films:</b> {{ category.movies|length }}</li>#}
{#        <li><b>Nombre de séries:</b> {{ category.series|length }}</li>#}

{#        {% set lastMovie = category.movies|last %}#}
{#        {% set lastSerie = category.series|last %}#}

{#        {% if lastMovie %}#}
{#            <li><b>Dernier film ajouté:</b> <a href="{{ path('movie_detail', {'id': lastMovie.id}) }}" class="underline decoration-red-600">{{ lastMovie.title }}</a>#}
{#            </li>#}
{#        {% endif %}#}

{#        {% if lastSerie %}#}
{#            <li><b>Dernière série ajoutée:</b> <a href="{{ path('movie_detail', {'id': lastSerie.id}) }}" class="underline decoration-red-600">{{ lastSerie.title }}</a>#}
{#            </li>#}
{#        {% endif %}#}
{#    </ul>#}
{% endblock %}